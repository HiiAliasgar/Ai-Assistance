[project]
name = "project1"
version = "0.1.0"
description = "Simple AI assistant using LangChain + Groq with tool-calling"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=0.3.27",
    "langchain-groq>=0.3.0",
    "langgraph>=0.2.0",
    "python-dotenv>=1.0.1",
]
